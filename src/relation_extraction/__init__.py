"""Extraction de relations (Jalon 4, complet) : interface commune
(base.py), structure Relation (relation.py), approche syntaxique
(dependency_extractor.py), approche LLM via Groq (llm_extractor.py),
comparaison qualitative (compare.py), et application du modèle retenu
(LLM) sur le corpus (pipeline.py)."""
