# Jalon 1 — Setup & Architecture
pydantic-settings>=2.4,<3
python-dotenv>=1.0,<2
pytest>=8.0,<9
pytest-cov>=5.0,<6

# Jalon 2 — Ingestion & Prétraitement
pdfplumber>=0.11,<1
python-docx>=1.1,<2
requests>=2.31,<3

# Les jalons suivants ajouteront ici : spacy, transformers, neo4j, pandas,
# networkx, openai (ou équivalent open-source), etc. — un jalon à la fois,
# pour ne jamais installer une dépendance avant d'en avoir besoin.
